export const SITE_URL = "https://workquora.com";

// Where the logged-in React/Vite SPA (auth, client, admin dashboards) lives.
// Same domain today, kept as its own constant so it's a one-line change if
// the SPA ever moves to a subdomain (e.g. app.workquora.com).
export const SPA_URL = "https://workquora.com";
